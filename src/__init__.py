"""
src — Telescope source packages.

Subpackages:
  detection  — moon detection (HoughCircles + brightness selection)
  control    — PID controller
  hardware   — camera and stepper motor drivers
  utils      — OpenCV overlay helpers
"""
